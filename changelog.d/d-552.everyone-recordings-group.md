### Changed
- Recording access now uses Nextcloud's virtual `everyone` group, giving new accounts the recordings Team-folder mount immediately without periodic membership reconciliation. Installers enabling per-participant access control must enable both Team folders and the Everyone Group app; existing `recording-viewers` ACLs are migrated without changing whether a recording is private or public.
