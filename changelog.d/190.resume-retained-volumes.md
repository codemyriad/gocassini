### Fixed
- `cassini dev stack up --resume` now recreates containers and mounts the existing Compose and ExApp data volumes after a normal `stack down`, instead of requiring the removed containers to still exist.
