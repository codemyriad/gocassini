### Fixed
- Fixed the `cassini-viewer` test suite failing on a clean checkout of `main` (`npm test` was red): the embedded shadow-root DOM test stub was missing a `style` object, so three `ensureShadowAppRoot` tests threw. No production code change.
