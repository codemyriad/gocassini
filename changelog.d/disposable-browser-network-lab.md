### Added
- Added a disposable Chrome network lab with live controls for latency, bandwidth, WebRTC packet loss, disconnection, and recording-upload-only throttling. The separate controls stay reachable during outages; browser profiles retain buffered audio for recovery.
