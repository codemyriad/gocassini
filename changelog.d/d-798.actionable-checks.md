### Added
- A health check that needs attention now says what to do about it, with the commands to do it where a command is what's needed — shown behind a disclosure so you only read them if you want them.
