### Added
- Added `linux/arm64` multi-arch support to the portable Cassini ExApp container image. Releases and pushed branches now publish multi-platform images (`linux/amd64` and `linux/arm64`) with bundled ARM64 sherpa-onnx runtime libraries and Parakeet ASR models.
