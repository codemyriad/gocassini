### Fixed
- Narrowing the meeting list by room or tag now applies to search results too. Searching and then picking a tag used to leave meetings from outside the filter in the list, which looked as though the filter had stopped working.
- Changing the room or tag while a search is active now re-runs the search instead of leaving the previous answer on screen.
