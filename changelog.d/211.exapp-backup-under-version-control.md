### Added
- `ops/cassini-exapp-backup.{sh,service,timer}`: the daily off-host restic backup of the production ExApp volume (D-568, D-576) now lives in the repository. It was running on the archive host and existed in no commit on any branch, so the only copy of a script guarding the production meeting data was the deployed file itself. The committed copy is byte-identical to what is installed.
