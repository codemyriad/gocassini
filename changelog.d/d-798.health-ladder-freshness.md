### Fixed
- A recording check that was passing no longer turns into "not verified" a few minutes later. It keeps what it found and says how long ago it found it, so an idle panel stops looking like a problem.
- A check that has never run now says so, instead of being reported the same way as one whose result has simply aged.
