### Fixed

- Fixed selected tags being silently replaced by an existing case-insensitive label match. Meetings can retain multiple selected tag IDs, including legacy case variants, and the tag vocabulary now combines the same ID across recording namespaces. [D-773]
