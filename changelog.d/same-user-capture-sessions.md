Support simultaneous Talk browsers signed in as the same Nextcloud user. Browser
capture IDs keep uploads and expected-upload waits independent; per-segment public
Talk session IDs match each upload to its own recorded track, including after a
reload. Mixing and transcription preserve each session's audio with shared user
attribution. Cassini shows capture and session identifiers alongside upload and
build evidence. Legacy ambiguous recordings retain the server-recorded audio.
