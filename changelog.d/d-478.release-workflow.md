### Added
- Documented the canonical release policy (tag-driven releases, changelog
  workflow, guarded Nextcloud App Store publishing) and added release
  automation: readiness checks, App Store tarball build/validation, a
  one-dispatch release workflow, and a package workflow that skips store
  upload until app registration completes.

### Fixed
- Declared the app license with its SPDX identifier (`AGPL-3.0-or-later`)
  in the ExApp manifest.
