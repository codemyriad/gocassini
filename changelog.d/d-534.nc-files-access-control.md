### Added
- **Per-participant access control for recordings.** The operator freezes each recording's audience to the Talk room's participants at publish time — it grants read-only Nextcloud advanced-ACL (`nc:acl-list`) to each local participant (users, groups, circles) on the meeting's `.opus`; guests, email and federated participants are skipped. The in-Nextcloud viewer is then served **per caller**: the meeting list is built directly from `catalog.json`, filtered to the `.opus` files visible in the caller's own Nextcloud scan, and each recording is fetched as the caller so Nextcloud enforces access — a recording a user is not granted never appears and cannot be played. The read path fails closed (an empty list, never the full archive) on any error. Access is editable afterwards through Nextcloud's own sharing/ACL tools. Recordings are owned by a dedicated `cassini` service account. See `docs/exapp-nextcloud-recordings-permissions.md`.
