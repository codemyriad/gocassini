### Added
- `cassini doctor --json` emits its checks as structured data, each with a stable id, for tools that need to read them. Running `cassini doctor` by hand prints exactly what it printed before.

### Fixed
- The operator no longer works out whether the optimised speech runtime is active by searching doctor's output for a particular sentence. Rewording that sentence used to silently break the check.
