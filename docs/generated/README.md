# Generated docs

Files in this tree are derivative outputs.

They should be regenerated from:

- `docs/source-of-truth/`
- `docs/structures/`
- `docs/docsets.yaml`

Manual edits here are possible, but the intended workflow is:

1. update source of truth
2. keep or revise the relevant structure
3. regenerate the output
