# Browser-facing harness host/IP is configured through CASSINI_HARNESS_HOST.
# harness/vm/bin/render-config.sh renders the host-aware runtime config under
# runtime/generated/ before Docker Compose starts the stack.
x-nextcloud-env: &nextcloud-env
  POSTGRES_HOST: db
  POSTGRES_DB: nextcloud
  POSTGRES_USER: nc
  POSTGRES_PASSWORD: nc
  NEXTCLOUD_ADMIN_USER: admin
  NEXTCLOUD_ADMIN_PASSWORD: admin
  NEXTCLOUD_TRUSTED_DOMAINS: "localhost 127.0.0.1 nextcloud host.docker.internal ${CASSINI_HARNESS_HOST:-127.0.0.1}"

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: nextcloud
      POSTGRES_USER: nc
      POSTGRES_PASSWORD: nc
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nc -d nextcloud"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - db_data:/var/lib/postgresql/data

  nextcloud:
    image: nextcloud:stable
    restart: unless-stopped
    environment: *nextcloud-env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "28080:80"
    volumes:
      - nextcloud_data:/var/www/html

  nats:
    image: nats:2.11-alpine
    profiles: ["full"]
    restart: unless-stopped
    network_mode: host
    command: ["-js", "-a", "127.0.0.1", "-p", "14222"]

  janus:
    image: mwalbeck/janus-gateway:latest
    profiles: ["full"]
    restart: unless-stopped
    network_mode: host
    depends_on:
      - nats
    volumes:
      - ./runtime/generated/janus/janus.jcfg:/opt/janus/etc/janus/janus.jcfg:ro
      - ./config/janus/janus.transport.websockets.jcfg:/opt/janus/etc/janus/janus.transport.websockets.jcfg:ro
      - ./config/janus/janus.transport.http.jcfg:/opt/janus/etc/janus/janus.transport.http.jcfg:ro
      - ./config/janus/janus.plugin.videoroom.jcfg:/opt/janus/etc/janus/janus.plugin.videoroom.jcfg:ro

  signaling:
    image: strukturag/nextcloud-spreed-signaling:latest
    profiles: ["full"]
    restart: unless-stopped
    network_mode: host
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - nats
      - janus
      - nextcloud
    volumes:
      - ./runtime/generated/signaling.conf:/config/server.conf:ro

  coturn:
    image: coturn/coturn:4.6-alpine
    profiles: ["full"]
    restart: unless-stopped
    network_mode: host
    command: ["-c", "/etc/coturn/turnserver.conf"]
    volumes:
      - ./runtime/generated/turnserver.conf:/etc/coturn/turnserver.conf:ro

volumes:
  db_data:
  nextcloud_data:
