# Agent Instructions

- For Go code, use the `go-coding-standard` skill in `.agents/skills/go-coding-standard/SKILL.md`. Keep detailed Go conventions there rather than duplicating them here.
- For standards-alignment refactors, use `/refactor-to-standard go-coding-standard <scope>`, where `<scope>` is repo-root-relative and `*` means the whole repo.
