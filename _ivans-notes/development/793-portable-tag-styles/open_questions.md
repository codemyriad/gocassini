# Open questions

No blocking questions remain for the simplified plan.

- Q1: download parity after archive saved remains explicitly accepted.
- Q2: ID-based offline restyle remains explicitly accepted.
- Q3: previous privileged global propagation is superseded. Styles follow existing rename scope: caller-accessible recordings only.
- Latest instruction drops central authority and reconciliation. `shaping.md` resolves the read-only compatibility transition, nullable fields, schema migration, attribution and persisted job envelope without changing the agreed scope.

Historical responses remain in `assessment.md`. Planning is complete; implementation has not started. The next session should use `handoff.md`. No new product preference is awaiting an answer.
